<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.Tab?>
<?import javafx.scene.control.TabPane?>
<?import javafx.scene.control.TableColumn?>
<?import javafx.scene.control.TableView?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.AnchorPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<TabPane fx:id="tabPane" maxWidth="600.0" prefHeight="400.0" prefWidth="600.0" styleClass="root-financeiro" xmlns="http://javafx.com/javafx/23.0.1" xmlns:fx="http://javafx.com/fxml/1" fx:controller="gui.TabelasAuxiliaresController">

    <!-- ABA SEXO -->
    <Tab text="Sexo">
        <AnchorPane>
            <VBox alignment="TOP_LEFT" spacing="10" AnchorPane.bottomAnchor="10.0" AnchorPane.leftAnchor="10.0" AnchorPane.rightAnchor="10.0" AnchorPane.topAnchor="10.0">
                <!-- Tabela de Sexo -->
                <TableView fx:id="tableSexo" prefHeight="200.0">
                    <columns>
                        <TableColumn fx:id="colSexoNome" prefWidth="578.399951338768" text="Nome" />
                    </columns>
                </TableView>

                <!-- Campo + Botoes -->
                <HBox spacing="10">
                    <Label text="Nome:" />
                    <TextField fx:id="txtSexoNome" prefHeight="26.0" prefWidth="533.0" />
                </HBox>
                <HBox prefHeight="26.0" prefWidth="507.0" spacing="10">
                    <Button fx:id="btnNovoSexo" onAction="#handleNovoSexo" prefHeight="26.0" prefWidth="86.0" text="Novo" />
                    <Button fx:id="btnSalvarSexo" onAction="#handleSalvarSexo" prefHeight="26.0" prefWidth="86.0" text="Salvar" />
                    <Button fx:id="btnExcluirSexo" onAction="#handleExcluirSexo" prefHeight="26.0" prefWidth="85.0" text="Excluir" textAlignment="CENTER" />
                </HBox>
            </VBox>
        </AnchorPane>
    </Tab>

    <!-- ABA TIPO DOC -->
    <Tab text="Tipo Doc">
        <AnchorPane>
            <VBox spacing="10" AnchorPane.bottomAnchor="10.0" AnchorPane.leftAnchor="10.0" AnchorPane.rightAnchor="10.0" AnchorPane.topAnchor="10.0">
                <TableView fx:id="tableTipoDoc" prefHeight="200.0">
                    <columns>
                        <TableColumn fx:id="colTipoDocNome" text="Nome" />
                    </columns>
                </TableView>
                <HBox spacing="10">
                    <Label text="Nome:" />
                    <TextField fx:id="txtTipoDocNome" />
                </HBox>
                <HBox spacing="10">
                    <Button fx:id="btnNovoTipoDoc" onAction="#handleNovoTipoDoc" text="Novo" />
                    <Button fx:id="btnSalvarTipoDoc" onAction="#handleSalvarTipoDoc" text="Salvar" />
                    <Button fx:id="btnExcluirTipoDoc" onAction="#handleExcluirTipoDoc" text="Excluir" />
                </HBox>
            </VBox>
        </AnchorPane>
    </Tab>

    <!-- ABA NACIONALIDADE -->
    <Tab text="Nacionalidade">
        <AnchorPane>
            <VBox spacing="10" AnchorPane.bottomAnchor="10.0" AnchorPane.leftAnchor="10.0" AnchorPane.rightAnchor="10.0" AnchorPane.topAnchor="10.0">
                <TableView fx:id="tableNacionalidade" prefHeight="200.0">
                    <columns>
                        <TableColumn fx:id="colNacionalidadeNome" text="Nome" />
                    </columns>
                </TableView>
                <HBox spacing="10">
                    <Label text="Nome:" />
                    <TextField fx:id="txtNacionalidadeNome" />
                </HBox>
                <HBox spacing="10">
                    <Button fx:id="btnNovoNacionalidade" onAction="#handleNovoNacionalidade" text="Novo" />
                    <Button fx:id="btnSalvarNacionalidade" onAction="#handleSalvarNacionalidade" text="Salvar" />
                    <Button fx:id="btnExcluirNacionalidade" onAction="#handleExcluirNacionalidade" text="Excluir" />
                </HBox>
            </VBox>
        </AnchorPane>
    </Tab>

    <!-- ABA PASSAGEM AUX -->
    <Tab text="Passagem Aux">
        <AnchorPane>
            <VBox spacing="10" AnchorPane.bottomAnchor="10.0" AnchorPane.leftAnchor="10.0" AnchorPane.rightAnchor="10.0" AnchorPane.topAnchor="10.0">
                <TableView fx:id="tablePassagemAux" prefHeight="200.0">
                    <columns>
                        <TableColumn fx:id="colPassagemAuxNome" text="Nome" />
                    </columns>
                </TableView>
                <HBox spacing="10">
                    <Label text="Nome:" />
                    <TextField fx:id="txtPassagemAuxNome" />
                </HBox>
                <HBox spacing="10">
                    <Button fx:id="btnNovoPassagemAux" onAction="#handleNovoPassagemAux" text="Novo" />
                    <Button fx:id="btnSalvarPassagemAux" onAction="#handleSalvarPassagemAux" text="Salvar" />
                    <Button fx:id="btnExcluirPassagemAux" onAction="#handleExcluirPassagemAux" text="Excluir" />
                </HBox>
            </VBox>
        </AnchorPane>
    </Tab>

    <!-- ABA AGENTE AUX -->
    <Tab text="Agente Aux">
        <AnchorPane>
            <VBox spacing="10" AnchorPane.bottomAnchor="10.0" AnchorPane.leftAnchor="10.0" AnchorPane.rightAnchor="10.0" AnchorPane.topAnchor="10.0">
                <TableView fx:id="tableAgenteAux" prefHeight="200.0">
                    <columns>
                        <TableColumn fx:id="colAgenteAuxNome" text="Nome" />
                    </columns>
                </TableView>
                <HBox spacing="10">
                    <Label text="Nome:" />
                    <TextField fx:id="txtAgenteAuxNome" />
                </HBox>
                <HBox spacing="10">
                    <Button fx:id="btnNovoAgenteAux" onAction="#handleNovoAgenteAux" text="Novo" />
                    <Button fx:id="btnSalvarAgenteAux" onAction="#handleSalvarAgenteAux" text="Salvar" />
                    <Button fx:id="btnExcluirAgenteAux" onAction="#handleExcluirAgenteAux" text="Excluir" />
                </HBox>
            </VBox>
        </AnchorPane>
    </Tab>

    <!-- ABA HORÁRIO SAÍDA -->
    <Tab text="Horário Saída">
        <AnchorPane>
            <VBox spacing="10" AnchorPane.bottomAnchor="10.0" AnchorPane.leftAnchor="10.0" AnchorPane.rightAnchor="10.0" AnchorPane.topAnchor="10.0">
                <TableView fx:id="tableHorarioSaida" prefHeight="200.0">
                    <columns>
                        <TableColumn fx:id="colHorarioSaidaNome" text="Nome" />
                    </columns>
                </TableView>
                <HBox spacing="10">
                    <Label text="Nome:" />
                    <TextField fx:id="txtHorarioSaidaNome" />
                </HBox>
                <HBox spacing="10">
                    <Button fx:id="btnNovoHorarioSaida" onAction="#handleNovoHorarioSaida" text="Novo" />
                    <Button fx:id="btnSalvarHorarioSaida" onAction="#handleSalvarHorarioSaida" text="Salvar" />
                    <Button fx:id="btnExcluirHorarioSaida" onAction="#handleExcluirHorarioSaida" text="Excluir" />
                </HBox>
            </VBox>
        </AnchorPane>
    </Tab>

    <!-- ABA ACOMODAÇÃO -->
    <Tab text="Acomodação">
        <AnchorPane>
            <VBox spacing="10" AnchorPane.bottomAnchor="10.0" AnchorPane.leftAnchor="10.0" AnchorPane.rightAnchor="10.0" AnchorPane.topAnchor="10.0">
                <TableView fx:id="tableAcomodacao" prefHeight="200.0">
                    <columns>
                        <TableColumn fx:id="colAcomodacaoNome" text="Nome" />
                    </columns>
                </TableView>
                <HBox spacing="10">
                    <Label text="Nome:" />
                    <TextField fx:id="txtAcomodacaoNome" />
                </HBox>
                <HBox spacing="10">
                    <Button fx:id="btnNovoAcomodacao" onAction="#handleNovoAcomodacao" text="Novo" />
                    <Button fx:id="btnSalvarAcomodacao" onAction="#handleSalvarAcomodacao" text="Salvar" />
                    <Button fx:id="btnExcluirAcomodacao" onAction="#handleExcluirAcomodacao" text="Excluir" />
                </HBox>
            </VBox>
        </AnchorPane>
    </Tab>

</TabPane>
